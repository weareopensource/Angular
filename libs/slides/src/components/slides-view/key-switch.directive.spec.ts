import { KeySwitchDirective } from './key-switch.directive';

describe('KeySwitchDirective', () => {
  it('should create an instance', () => {
    const directive = new KeySwitchDirective();
    expect(directive).toBeTruthy();
  });
});
